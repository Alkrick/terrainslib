from .parameters import *

# from hydra.core.config_store import ConfigStore

# cs = ConfigStore.instance()

# cs.store(group="parameter", name="range", node=Range)
# cs.store(group="parameter", name="normal", node=Normal)
# cs.store(group="parameter", name="constant", node=Constant)
# cs.store(group="parameter", name="choice", node=Choice)